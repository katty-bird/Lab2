package closeFarProject;

public class CloseFar {
	
	public static void main (String[] args) {
		int a = 1;  // change a, b, c for testing
		int b = 2;
		int c = 3;
		boolean result = calculateCloseFar(a, b, c);
		System.out.println(result);
	}
	
	public static boolean calculateCloseFar(int a, int b, int c) {
		 throw new java.lang.UnsupportedOperationException("Not implemented yet.");
		 // The method returns true, if b or c is differing from a by at most 1, 
		 // while the other is differing from both other values by 2 or more. 
		 // Note: Math.abs(num) computes the absolute value of a number.
		 
		 // Otherwise returns false.
	}
}